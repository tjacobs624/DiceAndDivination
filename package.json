{
  "name": "dnd-discord-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Remote MCP server on Cloudflare Workers that lets Claude read a Discord D&D channel for context, protected by Cloudflare Access (Managed OAuth).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "^0.23.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.30.0"
  }
}
